[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "digital-sensei-shared-models"
version = "0.1.0"
description = "Общие модели данных"
dependencies = [
    "pydantic>=2.0.0",
    "python-dotenv>=1.0.0"
]